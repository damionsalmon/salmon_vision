// No renderer-facing APIs are exposed yet. Add them here via
// contextBridge.exposeInMainWorld(...) if/when the app needs native desktop
// integration (file system, native menus, auto-update, etc).
